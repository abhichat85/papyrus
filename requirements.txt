# Dependencies for the Vercel Python function (api/index.py).
# The canonical dependency list is pyproject.toml; this mirrors the
# runtime + api extras, minus anything only the CLI or tests need.
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
chardet>=5.2.0
pyyaml>=6.0
fastapi>=0.115.0
python-multipart>=0.0.20
